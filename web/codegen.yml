overwrite: true
schema: "graphql/schema.gql"
documents: "graphql/**/*.gql"
generates:
  graphql/generate.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true